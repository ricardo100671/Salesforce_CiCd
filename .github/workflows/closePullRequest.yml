name: Close Pull Request
on:
  pull_request:
jobs:
  ClosePullRequest:
    runs-on: ubuntu-latest
    steps:
      - uses: ricardo100671/salesforce_cicd/closePullRequest@main
        with:
          sfConsumerKey:
          path: force-app/**