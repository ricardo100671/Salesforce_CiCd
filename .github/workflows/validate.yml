name: Validate Pull Request
on: 
  pull_request:
jobs:
  Validate:
    runs-on: ubuntu-latest
    steps:
      - uses: ricardo100671/salesforce_cicd/validate@main
        with:
          sfConsumerKey: 
          path: force-app/**